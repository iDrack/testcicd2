name: wf on push
on: push
jobs:
    mon_job:
        name: build test run
        runs-on: ubuntu-latest
        steps:
            - name: install npm
              run: npm ci
            - name: build
              run: npm run build
            - name: test
              run: npm run test
            - name: run 
              run: npm run start